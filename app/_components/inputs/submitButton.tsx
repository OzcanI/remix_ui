export const SubmitButton = ({text}: {text: string}) => {
    return <button type="submit" className="w-full py-[8px] text-center bg-dark-sky-blue text-white mt-[35px] rounded-[4px] mb-[63px]">
        {text}
    </button>
}